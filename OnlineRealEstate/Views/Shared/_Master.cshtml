<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" type="text/css" href="~/Styles/Master.css" />
    <title>@ViewBag.Title</title>
</head>
<body>
    <header class="header" style="padding : 10px;">
        <div>
           
            <nav style="float:right;">
                @*<h2 class="link">
                @Html.ActionLink("Home", "Index", "Home")
                @Html.ActionLink("SignUp", "SignUp", "Account")
                @Html.ActionLink("SignIn", "Login", "Account")
            </h2>*@
            </nav>
            @if (HttpContext.Current.User.Identity.IsAuthenticated)
            {
                @Html.ActionLink("LogOut", "LogOut", "Account")
            }
            else
            {
                @Html.ActionLink("SignIn", "Login", "Account")
            }
            @if (HttpContext.Current.User.IsInRole("Admin"))
            {
                @Html.ActionLink("Property details", "Create", "Property")
                @Html.ActionLink("Property details", "DisplayPropertyDetails", "Property")
            }
            <h1 class="headerText">Online Mobile Shopping</h1>



        </div>

    </header>
    <div>
        @RenderBody()
    </div>
</body>
</html>
